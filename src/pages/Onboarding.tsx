/**
 * Onboarding Page
 *
 * Shown after successful signup to connect calendar and populate CRM.
 */

import { OnboardingFlow } from '@/components/onboarding/OnboardingFlow';

export default function Onboarding() {
  return <OnboardingFlow />;
}
